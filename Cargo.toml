[package]
name = "entros-mopro"
version = "0.1.0"
edition = "2021"
description = "Mobile Groth16 prover for Entros Poseidon openings and bounded Hamming distance."
license = "MIT"
repository = "https://github.com/entros-protocol/entros-mopro"
documentation = "https://github.com/entros-protocol/entros-mopro#readme"
readme = "README.md"
keywords = ["zk", "groth16", "circom", "solana", "react-native"]
categories = ["cryptography", "wasm", "api-bindings"]

[lib]
crate-type = ["lib", "cdylib", "staticlib"]

[features]
default = ["uniffi"]
uniffi = ["mopro-ffi/uniffi"]
flutter = ["mopro-ffi/flutter"]
wasm = ["mopro-ffi/wasm"]
request-bound-v1 = []

[dependencies]
ark-bn254 = "0.5"
mopro-ffi = { version = "0.3.4" }
thiserror = "2.0.12"
anyhow = "1.0.99"
circom-prover = "0.1"
rust-witness = "0.1"
num-bigint = "0.4.0"
sha2 = "0.10.9"
serde_json = "1.0.94"

[build-dependencies]
witnesscalc-adapter = "0.1"
rust-witness = "0.1"
sha2 = "0.10.9"

[dev-dependencies]
mopro-ffi = { version = "0.3.4", features = ["uniffi-tests"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0.94"

[target.wasm32-unknown-unknown.dependencies]
mopro-ffi = { version = "0.3.4", features = ["wasm"] }
wasm-bindgen = "0.2"
serde-wasm-bindgen = "0.6"
